# Web3 Bounty Hunter — Discovery Layer
# pip install -r requirements.txt

# HTTP
httpx>=0.27.0
aiohttp>=3.9.0

# Redis (async)
redis[asyncio]>=5.0.0

# Bloom Filter (fast hashing)
mmh3>=4.1.0

# Task queue (για Layer 2 workers)
celery>=5.3.0
arq>=0.26.0          # lightweight async task queue

# Data / utilities
python-dotenv>=1.0.0
pydantic>=2.0.0       # optional: για validation

# Web scraping fallback (για πλατφόρμες χωρίς API)
playwright>=1.44.0    # pip install playwright && playwright install chromium
beautifulsoup4>=4.12.0

# Monitoring
structlog>=24.0.0
